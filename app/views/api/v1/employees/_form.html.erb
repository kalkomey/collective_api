<%= form_for(@api_v1_employee) do |f| %>
  <% if @api_v1_employee.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@api_v1_employee.errors.count, "error") %> prohibited this api_v1_employee from being saved:</h2>

      <ul>
      <% @api_v1_employee.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
